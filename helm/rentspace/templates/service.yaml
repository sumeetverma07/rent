apiVersion: v1
kind: Service
metadata:
  name: {{ include "rentspace.fullname" . }}
  labels:
    {{- include "rentspace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rentspace.selectorLabels" . | nindent 4 }}
